using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class spawnPointHunter : MonoBehaviour
{
  /*  bool spawn = false;
    PlayerControls spawnControls;
    [SerializeField]
    Transform spawnPoint;
    [SerializeField]
    GameObject Hunter;
    [SerializeField]
    GameObject spawnPointEnable;

    // Start is called before the first frame update
    private void Awake()
    {
        spawnControls = new PlayerControls();

        spawnControls.Gameplay.Spawn.performed += ctx => SpawnHunter();

        spawnPointEnable = GameObject.FindGameObjectWithTag("SpawnPoint");
    }

    // Update is called once per frame
    void Update()
    {
        SpawnHunter();
    }

    public void SpawnHunter()
    {

        print("Spawned");

        spawn = true;

        if (spawn == true)
        {
            Hunter.transform.position = spawnPoint.transform.position;

            spawn = false;

            spawnPoint.GetComponent<spawnPointHunter>().enabled = false;
        }

    }
    */
}
